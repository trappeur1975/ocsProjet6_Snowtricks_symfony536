<form method="post" action="{{ path('picture_delete', {'id': picture.id}) }}" onsubmit="return confirm('êtes vous sur de vouloir supprimer cette picture?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
    <button class="btn btn-danger btn-sm bi bi-trash">Delete</button>
    {# <button class="btn">Delete</button> #}
</form>
