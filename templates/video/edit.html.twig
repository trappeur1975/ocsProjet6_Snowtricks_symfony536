{% extends 'base.html.twig' %}

{% block title %}Modifier la video "{{video.videoFileName}}{% endblock %}

{% block body %}
    <h1>Edit Video</h1>

    {{ include('video/_form.html.twig', {'button_label': 'Modifier'}) }}

    {# {% if app.user is same as picture.trick.user %} #}   
        <div class="btn-group" role="group" aria-label="gestion du trick">
            <a href="{{ path('trick_show', {'slug': video.trick.slug}) }}"><i class="btn btn-success btn-sm bi bi-eye-fill"></i></a>
            {# <a href="{{ path('video_index') }}">back to list</a> #}
            {# {{ include('video/_delete_form.html.twig') }} #}
        </div>
    {# {% endif %} #}

    
{% endblock %}
