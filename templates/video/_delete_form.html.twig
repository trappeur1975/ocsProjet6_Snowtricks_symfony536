<form method="post" action="{{ path('video_delete', {'id': video.id}) }}" onsubmit="return confirm('êtes vous sur de vouloir supprimer cette video?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video.id) }}">
    <button class="btn btn-danger btn-sm bi bi-trash">Delete</button>
    {# <button class="btn">Delete</button> #}
</form>
